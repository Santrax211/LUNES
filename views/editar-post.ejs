<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Post</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="icon" href="/iconweb.png" type="image/png">
</head>
<body>
    <header>
        <h1>Editar Post</h1>
        <nav>
            <ul>
                <li><a href="/">Inicio</a></li>
                <li><a href="/nuevo-post">Nuevo Post</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <form action="/editar-post/<%= post.id %>" method="POST" class="form-post" enctype="multipart/form-data" data-netlify="true">
            <label for="titulo">Título:</label>
            <input type="text" name="titulo" id="titulo" value="<%= post.titulo %>" required><br>

            <label for="contenido">Contenido:</label>
            <textarea name="contenido" id="contenido" rows="10" required><%= post.contenido %></textarea><br>

            <label for="autor">Autor:</label>
            <input type="text" name="autor" id="autor" value="<%= post.autor %>" required><br>

            <% if (post.imagen) { %>
                <div class="image-preview">
                    <p>Imagen actual:</p>
                    <img src="/uploads/<%= post.imagen %>" alt="Imagen del post" style="max-width: 100%; height: auto;">
                    <input type="checkbox" name="removeImage" id="removeImage">
                    <label for="removeImage">Eliminar imagen</label><br>
                </div>
            <% } %>

            <label for="imagen">Nueva Imagen:</label>
            <input type="file" name="imagen" id="imagen" accept="image/*"><br>

            <button type="submit" class="btn-submit">Actualizar</button>
        </form>
    </main>
</body>
</html>


